import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
   allowedDevOrigins: ['svc-bafc2899-morphvm-nv859g8p.http.cloud.morph.so'],
};

export default nextConfig;
